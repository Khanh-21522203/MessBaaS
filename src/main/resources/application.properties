server.port=8082
server.bossThreads=1
server.workerThreads=0
server.businessThreads=0

db.url=jdbc:mysql://localhost:3306/mess_baas
db.username=root
db.password=mysql
db.driverClassName=com.mysql.cj.jdbc.Driver

message.hotBufferPerChannel=2048

redis.enabled=false
redis.host=localhost
redis.port=6379
redis.timeoutMillis=2000

projection.pollMillis=200
projection.batchSize=100
projection.maxAttempts=10
projection.baseBackoffMillis=200
projection.leaseMillis=5000

inbox.defaultLimit=50
inbox.maxLimit=200
